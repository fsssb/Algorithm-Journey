#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    /**
     * @brief 组合综合实现
     * 核心逻辑：排序 + DFS + 回溯剪枝
     */
    vector<vector<int>> combinationSum(vector<int>& candidates, int target) {
        
    }
};